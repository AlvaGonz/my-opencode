id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Create an animated hero section with staggered fade-in using anime.js."
expected:
  outcomes:
    - type: task_completed
should_trigger: true
