id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Implement TDD workflow with git commits and checkpoint verification."
expected:
  outcomes:
    - type: task_completed
