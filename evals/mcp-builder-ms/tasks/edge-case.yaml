id: edge-case-001
name: Edge Case - Secondary Trigger
description: |
  Test the skill with a variation of the primary trigger pattern.
tags:
  - edge-case
  - positive-trigger
inputs:
  prompt: "Use this skill when building MCP servers to integrate external APIs or services, whether in Python (FastMCP) or Node/Typ..."
expected:
  outcomes:
    - type: task_completed
