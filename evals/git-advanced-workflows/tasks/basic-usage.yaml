id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Use interactive rebase to squash these 5 commits into a clean history."
expected:
  outcomes:
    - type: task_completed
