id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Build a single-file HTML artifact showing an interactive data dashboard."
expected:
  outcomes:
    - type: task_completed
should_trigger: true
