id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Refactor this component to use compound components instead of boolean props."
expected:
  outcomes:
    - type: task_completed
should_trigger: true
