id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Design a memory retrieval system that surfaces the right context for an AI agent."
expected:
  outcomes:
    - type: task_completed
