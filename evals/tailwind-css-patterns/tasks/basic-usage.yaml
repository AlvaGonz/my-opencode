id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Build a responsive card grid component using Tailwind CSS utilities."
expected:
  outcomes:
    - type: task_completed
should_trigger: true
