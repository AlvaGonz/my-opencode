id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Design a Temporal saga pattern for a multi-step booking workflow."
expected:
  outcomes:
    - type: task_completed
