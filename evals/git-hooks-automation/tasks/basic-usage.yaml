id: basic-usage-001
name: Basic Usage - Positive Trigger
description: |
  Test that the skill activates and completes its primary use case.
tags:
  - basic
  - happy-path
  - positive-trigger
inputs:
  prompt: "Set up husky with commitlint and lint-staged for this project."
expected:
  outcomes:
    - type: task_completed
should_trigger: true
